# Convenience shim so `pip install -r requirements.txt` sets the project up.
#
# It installs this repo in editable mode, which pulls in the dependency list
# declared in pyproject.toml. Add or change dependencies there, not here.
-e .
